import { createGlobalStyle } from 'styled-components'; // Importando o createGlobalStyle

// Estilo global para a aplicação
export const GlobalStyle = createGlobalStyle`
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
`;
